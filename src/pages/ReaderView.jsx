import React, { Fragment } from 'react';
import { Reader } from 'component/Reader';

const ReaderView = () => {
  return (
    <Fragment>
      <Reader />
    </Fragment>
  );
};

export default ReaderView;
