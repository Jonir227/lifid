import React, { Fragment } from 'react';
import { Reader } from 'components';

const ReaderView = () => {
  return (
    <Fragment>
      <Reader />
    </Fragment>
  );
};

export default ReaderView;
